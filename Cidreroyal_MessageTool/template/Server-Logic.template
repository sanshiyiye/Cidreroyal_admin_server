/**
 * {{ msgBean.name }}逻辑处理
 * Created by auto tool.
 */

'use strict';

// {{ msgBean.comment }}
module.exports = function (reqData, req, res, next) {
  var msg = new JF.msg.{{ msgBean.package }}{{ msgBean.name }}Msg(reqData);

  return null;
};